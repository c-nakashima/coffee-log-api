# Coffee API

FastAPI backend for coffee logging app.

## Setup

```bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

# Run

uvicorn app.main:app --reload

# API Docs

Open:
http://127.0.0.1:8000/docs
